# CI/CD Configuration Contract for kontomierz-mcp
#
# This file is the Single Source of Truth for all CI/CD parameters.
# Workflow files (.github/workflows/*.yml) are generated from this config.
#
# Archetype: Python + MCP + Docker + src/ (Variant A)
# Standard: ci-cd-standard.md v2.0.0

language: "python"

src_dir: "src/kontomierz_mcp/"
package: "kontomierz_mcp"
project: "kontomierz-mcp"
python_version: "3.14"

image_name: "kontomierz-mcp:test"
smoke_name: "kontomierz-mcp-smoke"
health_port: 9100
rest_port: 9102

use_docker: true
is_mcp: true
expected_tools: 27
dockerfile_target: ""

use_codecov: true
use_semgrep: true
use_dependabot: true
package_ecosystems:
  - "github-actions"
  - "pip"

use_docs_validation: false
services: null
